# Embedding
EMBEDDING_MODEL=all-MiniLM-L6-v2

# Storage paths
CHROMA_PATH=data/chroma
SQLITE_BM25_PATH=data/bm25.db
SQLITE_CHAT_PATH=data/chat.db

# Chat (optional — if set, chat routes are enabled)
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini
